Для запуска собрать каждый сервис (mvn clean package), потом запустить ```docker-compose.yml```.

Есть неисправленная проблема с тем, что первый запрос после запуска выдает ошибку 503 или 504 (хотя post/put/delete запросы исполнятся правильно), все следующие запросы ошибок не выдают. 
